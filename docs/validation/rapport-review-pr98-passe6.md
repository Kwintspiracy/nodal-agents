- **R1 — majeur — `apps/cli/src/lib/postgres.ts:231`, `:238`, `:205`. Le constat tient : une erreur d’origine peut autoriser un étranger.** Déclenchement : fichier périmé, PID recyclé et recul de l’horloge système. `btime` provient de `getboottime64`, dont Linux précise que la valeur change avec `settimeofday`. Exemple reproduit en mémoire avec le calcul actuel : démarrage initial `B`, ancien postmaster à `B+1000`, étranger réutilisant son PID à `B+2000`, puis recul de l’horloge de 1000 secondes. Le calcul retourne exactement `B+1000` et le classificateur retourne ce PID dans `owned`. La tolérance de deux secondes ne protège donc pas systématiquement. Aucun recyclage ni changement d’horloge réel effectué. [Production de `btime`](https://raw.githubusercontent.com/torvalds/linux/v6.12/fs/proc/stat.c), [comportement de `getboottime64`](https://raw.githubusercontent.com/torvalds/linux/v6.12/kernel/time/timekeeping.c).

- **R2 — majeur, persistant — `apps/cli/src/commands/up.ts:303`, `:331`, `:465`. Le constat tient : les workers restent omis lorsque le postmaster entre par le listener ou le fichier.** L’ajout du postmaster rend fausse la condition entourant l’ajout de tout `ownedPgPids`. Si un worker survit à l’arrêt, il n’est ni traité ni vérifié ; l’absence de port occupé permet néanmoins « Orphans cleaned up ». Le changement de message ne corrige pas ce volet du R4 précédent.

- **R3 — modéré — `apps/cli/src/commands/up.ts:315`. Le constat tient : le motif affiché peut être faux.** Déclenchement : échec WMI sous Windows, lecture `/proc` impossible ou date absente. Le programme affirme une différence entre deux dates alors qu’il n’a pas pu les comparer. Il faut distinguer « date indisponible » et « dates différentes », ou annoncer simplement une identité non confirmée.

- **R4 — modéré, couverture — `apps/cli/src/tests/postmaster-pid.test.ts:165`, `apps/cli/src/commands/up.ts:386`. Le constat tient : les nouveaux tests ne verrouillent pas tous les correctifs.** Aucun test CLI trouvé n’exerce `runUp` ou `postgresProcessesForDataDir`. Rétablir le repli fondé sur la seule vivacité, sortir la revérification de la boucle ou rétablir l’ancien message échappe aux assertions présentes. Le test Linux accepte également une date de création remplacée par `Date.now()` après les lectures réussies : sa fenêtre de 24 heures ne valide pas la précision nécessaire de deux secondes.

1. **tient** — L’index est correct. Après le dernier `)`, les champs sont : `state[0]`, `ppid[1]`, `pgrp[2]`, `session[3]`, `tty_nr[4]`, `tpgid[5]`, `flags[6]`, `minflt[7]`, `cminflt[8]`, `majflt[9]`, `cmajflt[10]`, `utime[11]`, `stime[12]`, `cutime[13]`, `cstime[14]`, `priority[15]`, `nice[16]`, `num_threads[17]`, `itrealvalue[18]`, **`starttime[19]`**. Le dernier `)` traite correctement les parenthèses dans le nom. Vérification contre le format produit par Linux et fixture exécutée en mémoire ; aucune lecture Linux réelle ici. `btime` est bien en secondes epoch. [Format Linux](https://raw.githubusercontent.com/torvalds/linux/v6.12/fs/proc/array.c).

   `100` n’est toutefois pas universel : **Alpha utilise 1024**. Ce n’est pas le `CONFIG_HZ` du noyau. Avec 1024, le calcul ajoute `10,24 × t` au lieu de `t`, soit une erreur de `9,24 × t` secondes : refus pratique du propre postmaster, hors démarrage extrêmement précoce. À horloge stable, cela éloigne aussi un PID recyclé plus tard de l’ancienne date. Aucun déploiement Alpha de cette application n’est établi ici. [Définition Alpha](https://git.zx2c4.com/linux-rng/commit/arch/alpha?id=f65bbf05392b44714ccdcc4b5b1bebfd471d2665). La valeur est accessible via `sysconf(_SC_CLK_TCK)` ; le commentaire affirmant qu’elle n’est pas exposée au processus est faux.

2. **constat** — Si **l’écart calculé** dépasse deux secondes, le refus tient. En revanche, une erreur de calcul ne garantit pas cet écart : R1 fournit un rapprochement exact avec une ancienne date. « Toute erreur fait refuser » est faux.

3. **tient** — Sous Windows, ce repli retourne bien `owned: []`. Une alternative existe : ouvrir un handle sur le PID et lire sa création avec `GetProcessTimes`, qui accepte notamment `PROCESS_QUERY_LIMITED_INFORMATION`. `Get-Process -Id …` et `StartTime` offrent une voie plus simple depuis PowerShell. Toute impossibilité doit conserver le refus ; conserver le même handle jusqu’au signal serait nécessaire pour supprimer la course au recyclage. Pour cette passe, rester sur le refus est acceptable. [API Microsoft](https://learn.microsoft.com/en-us/windows/win32/api/processthreadsapi/nf-processthreadsapi-getprocesstimes).

4. **tient** — La revérification est désormais **par candidat avant `SIGKILL`**. Elle supprime l’attente du candidat précédent après une lecture commune. Elle ne corrige pas la fenêtre de l’arrêt gracieux, déjà relevée en passe 5.

   Le nombre de processus n’a pas de petite borne universelle : PostgreSQL autorise habituellement 100 connexions, auxquelles s’ajoutent workers et processus auxiliaires, avec des limites configurables. [Connexions PostgreSQL](https://www.postgresql.org/docs/18/runtime-config-connection.html). Ici, R2 réduit généralement la liste à un seul postmaster ; si la troisième voie ajoute `N` candidats, les sondes seules peuvent consommer environ **`N × 10 s`**, plus les attentes après signal. **NON TRANCHÉ** pour le coût réel, non mesuré. Une échéance globale avec refus explicite serait préférable à réutiliser une table vieillissante.

5. **constat** — Le dossier, le PID et la consigne de vérifier avant de tuer améliorent le message. Le motif est faux dans les cas R3. « Check that pid yourself » reste peu précis : indiquer de vérifier l’exécutable, la date et le cluster propriétaire rendrait l’action compréhensible.

6. **constat** — Le premier cas est réel : R2 persiste. Pour le second, la reproduction avec le classificateur actuel donne `[8932,5856]` avec le parent, puis `[]` sans lui. Refuser les workers sans preuve suffisante est acceptable ; annoncer leur nettoyage sans l’avoir vérifié ne l’est pas.

7. **constat** — Le test `tableRead: false` détecte bien le retour à l’ancienne attribution : résultat actuel vérifié en mémoire, `owned: []`. Les tests Linux vérifient une présence et une plausibilité grossière, pas la précision du calcul. Il manque les tests du repli complet, de la revérification par PID et des motifs affichés. Suite Vitest complète non exécutée dans cette sandbox en lecture seule ; vérifications ciblées uniquement, sans signal envoyé.

8. **tient** — Le test exigeant de posséder un PID sur la seule foi du fichier a été remplacé. Aucun autre test examiné n’exige explicitement cette attribution dangereuse. Les tests de `livePostmasterPid` vérifient seulement la vivacité, pas une autorisation de tuer. R3 de la passe 5 — boucle non PostgreSQL, `taskkill /T`, `sweepRecordedChildren` et `down.ts` : **hors périmètre, déjà noté**.

des constats