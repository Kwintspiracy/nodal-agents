- **R1 — majeur — `apps/cli/src/commands/up.ts:385`, `apps/cli/src/lib/postgres.ts:420`. Le constat tient : l’arrêt gracieux initial échappe à la reconfirmation.** Si le postmaster disparaît après la sonde de `up.ts:257` et que son PID est recyclé, le fichier périmé conserve le numéro attendu. `stopOrphanPostgres` vérifie seulement l’égalité du PID, puis appelle `pg_ctl` sans reconfirmer le processus. La lecture fraîche à `up.ts:398` arrive après cet arrêt. Refaire la confirmation avant l’arrêt gracieux, au plus près de son invocation ; cela réduit la fenêtre sans supprimer la course inhérente aux PID.

- **R2 — majeur — `apps/cli/src/lib/postgres.ts:89`, `:268`. Le constat tient : R1 de passe 8 n’est corrigé qu’en partie.** Sous Linux, `sameDirectory` transforme encore les antislashs en séparateurs. Deux répertoires distincts, `/srv/pg\data` et `/srv/pg/data`, comparent donc égaux, même après `realpathSync`. Vérification en mémoire de la fonction actuelle : résultat `true`. Avec recyclage du PID et concordance temporelle, la preuve par répertoire peut encore accepter un étranger. Préserver aussi les antislashs sous Linux.

- **R3 — modéré — `apps/cli/src/commands/up.ts:400`, `:482`, `:492`. Le constat tient : les morts restent comptés comme laissés vivants.** Après un arrêt gracieux réussi, une reconfirmation vide ajoute toujours le PID à `leftAlone`, sans vérifier sa vivacité. Le filtre annoncé dans la demande et le message de commit est absent de `2b205cb8` comme du code actuel. La branche `stillHeld` affiche désormais cette liste, mais elle peut elle aussi annoncer des processus morts comme « left running ».

- **R4 — modéré — `apps/cli/src/tests/postmaster-pid.test.ts:119`, `:195`. Le constat tient : deux assertions peuvent faire échouer une implémentation correcte sous Linux.** Le test `accepts the same directory written with the other separators and case` exige toujours l’acceptation du chemin en majuscules, désormais refusé. Le test de datation exige aussi un processus âgé de plus de trois secondes sans organiser cette attente : une exécution ciblée rapide peut échouer avant la comparaison utile. Adapter l’attente de casse à la plateforme et garantir l’âge avant la mesure.

- **R5 — modéré, documentation des garanties — `apps/cli/src/lib/postgres.ts:52`, `apps/cli/src/lib/orphans.ts:176`. Le constat tient : le résidu Windows est trop étroitement décrit.** La condition effective est une création située à **±2 secondes** de la date enregistrée, pas nécessairement au même instant. Un recyclage suffisamment rapide peut donc être accepté sans recul d’horloge ; le classificateur actuel accepte une différence de `+1 500 ms`, vérifiée en mémoire. Un recul n’a pas non plus besoin d’être délibéré. Pour le scénario décrit, préciser la concordance dans cette fenêtre et une lecture WMI exploitable ; conserver également le cas du recyclage rapide.

- **R6 — modéré, documentation — `apps/cli/src/lib/orphans.ts:96`, `apps/cli/src/lib/postgres.ts:206`, `apps/cli/src/commands/up.ts:327`. Le constat tient : les contradictions relevées en passe 8 subsistent.** « strictly after » décrit l’inverse de `parent.startedAt <= child.startedAt` ; le résumé du repli présente encore la vivacité comme suffisante ; la troisième voie est toujours annoncée comme conditionnelle alors que l’ajout est systématique. Le rapport de passe 8 contient bien ce cinquième constat, R5, absent du récapitulatif de la demande.

1. **constat** — Oui, l’arrêt gracieux initial constitue encore ce chemin : R1. La boucle `SIGKILL` reconfirme chaque PID ; `leftAlone` et `stillHeld` n’envoient aucun signal supplémentaire, mais leur bilan reste incorrect : R3.

2. **tient** — Le constat de contamination entre fichiers est faux dans la configuration actuelle : `apps/cli/vitest.config.ts` impose `fileParallelism: false`, et le pool par défaut utilise des processus séparés. Sous `threads`, `process.chdir()` est indisponible, plutôt qu’une modification concurrente silencieuse. [Documentation Vitest](https://vitest.dev/config/pool). Pour rendre ces tests indépendants du pool, lancer un processus auxiliaire avec `{ cwd: dataDir }`, écrire son PID et sa date dans le fichier, puis vérifier les deux cas temporels depuis le parent.

3. **constat** — R5. Le scénario de recul avant création tient, mais ses trois conditions ne bornent pas tous les cas acceptés. La fermeture Linux annoncée reste également imparfaite à cause de R2.

4. **constat** — R6, ainsi que les garanties temporelles trop absolues signalées en R5.

5. **tient** — Aucun export sans appel identifié dans le périmètre. `unconfirmedReading`, `processStartedAtMs` et `resolvePgCtl` servent en production, mais uniquement à l’intérieur de `postgres.ts` ; leur export sert aux tests directs. Ce choix ne constitue pas, à lui seul, un défaut nécessitant leur privatisation.

6. **constat** — Le comportement dangereux « comparaison insensible à la casse dans la preuve Linux du répertoire » pourrait être réintroduit **uniquement dans `postmasterHoldsDataDir`** sans être détecté par les tests du repli : `owns nothing when the process does not run out of our data directory` utilise des chemins entièrement différents. Le nouveau test `folds case only where the filesystem does` vérifie la lecture du fichier, pas cette preuve. Il manque un cas de repli avec deux répertoires distincts différant seulement par la casse, dates concordantes.

Vérifications en lecture seule et évaluations en mémoire, sans signal envoyé. Suite Vitest non exécutée ; scénarios Linux et recyclage réel non reproduits. Les cinq fichiers examinés sont identiques entre `2b205cb8` et le HEAD actuel.

des constats