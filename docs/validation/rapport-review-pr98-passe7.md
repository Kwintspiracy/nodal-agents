- **R1 — majeur, persistant — `apps/cli/src/lib/postgres.ts:232–250`. Le constat tient : le recul d’horloge peut toujours faire reconnaître un PID étranger.** Déclenchement : ancien postmaster démarré à `B+1000 s`, PID recyclé à `B+2000 s`, puis horloge reculée de `1000 s`. À l’uptime `3000 s`, l’heure courante vaut `B+2000 s` ; le nouveau calcul retourne `B+2000−(3000−2000) = B+1000 s`, exactement la date du fichier périmé. Reproduction en mémoire avec le classificateur actuel : `owned: [8932]`. Le remplacement de `btime` ne ferme donc pas R1 ; son commentaire promet à tort un refus systématique.

- **R2 — majeur, persistant en partie — `apps/cli/src/commands/up.ts:395–400`, `:474`. Le constat tient : un worker survivant peut encore être annoncé nettoyé sans vérification.** L’ajout systématique corrige son omission initiale. Mais si l’arrêt gracieux supprime le postmaster et son fichier, la nouvelle lecture ne confirme plus le worker : `continue` évite aussi sa vérification de vivacité. Sans port restant, « Orphans cleaned up » s’affiche. Reproduction du classificateur : `[8932,5856]` avant, `[]` après disparition du parent et du fichier. Refuser de tuer reste correct ; annoncer le nettoyage ne l’est pas.

- **R3 — modéré — `apps/cli/src/tests/postmaster-pid.test.ts:238–260`. Le constat tient : le nouveau test exige une attribution incorrecte sous Linux lorsque le processus a plus de quelques secondes.** Il écrit l’heure **courante** dans le fichier, puis attend `[process.pid]` dès que le processus peut être daté. Pour un processus âgé de dix secondes, le code correct retourne `[]`. Inversement, remplacer sa date par `Date.now()` satisfait cette assertion. Le résultat attendu doit dépendre de la concordance des dates, pas seulement de leur disponibilité.

- **R4 — modéré, documentation des garanties — `apps/cli/src/lib/orphans.ts:40`, `apps/cli/src/lib/postgres.ts:15`, `:99`, `:217`, `apps/cli/src/commands/up.ts:327`. Le constat tient : plusieurs commentaires contredisent encore le code.** Une date parent/enfant manquante interdit désormais la descendance, contrairement à « the check simply does not apply » ; `read: false` peut accompagner un `owned` confirmé par le repli ; la sonde n’attribue plus par ligne de commande ; la datation ne lit plus `btime` ; la troisième voie n’est plus conditionnée à l’échec des deux premières.

1. **tient** — L’arithmétique et les unités sont correctes avec `USER_HZ=100` : `200000 ticks / 100 = 2000 s` ; avec `uptime=3000 s`, l’âge est `1000 s`, donc on retranche `1000000 ms`. `/proc/uptime` utilise **CLOCK_BOOTTIME**, en secondes, veille comprise ; le champ `starttime` dérive également de `start_boottime`. Une suspension entre création et lecture augmente l’heure courante et l’âge de la même durée : **aucune dérive due à la veille seule**, ni vers le passé ni vers le futur. [Production d’uptime](https://raw.githubusercontent.com/torvalds/linux/v6.12/fs/proc/uptime.c), [production de starttime](https://raw.githubusercontent.com/torvalds/linux/v6.12/fs/proc/array.c), [horloges Linux](https://www.kernel.org/doc/html/latest/core-api/timekeeping.html).

2. **constat** — Oui : R1 donne un rapprochement exact. BOOTTIME résiste au changement d’heure, mais `Date.now()` le réintroduit dans l’estimation. Algébriquement, `now − uptime + starttime/100` conserve une origine murale mobile. Même sans saut d’horloge, deux générations espacées de moins de deux secondes restent acceptables pour ce prédicat : la tolérance n’est pas une preuve absolue d’identité.

3. **tient** — L’ajout systématique n’introduit pas de nouvelle source d’attribution. `ownedPgPids` vient exclusivement de `postgresProcessesForDataDir().owned`. Sous Windows, WMI filtre `postgres.exe`, puis `ownedPostgresPids` confirme le PID et sa date depuis le fichier, et parcourt les ascendances datées. Le repli ne fournit que le PID du fichier, vivant et daté, sans workers. La boucle ajoute ces PID en dédupliquant ; elle hérite toutefois de R1 et laisse subsister R2.

4. **tient** — Un test léger est possible, mais une fonction pure `shouldKill(pid, owned)` ne verrouillerait pas la **fraîcheur par candidat**. Extraire la petite boucle de nettoyage avec dépendances injectées (`readOwnership`, `isAlive`, `kill`, `wait`) permettrait de simuler deux candidats : le second perd son attribution pendant l’attente du premier. Vérifier les PID réellement transmis à `kill` détecterait une lecture déplacée avant la boucle, sans harnais complet de `runUp`.

5. **constat** — Oui : R4, plus la garantie erronée sur les sauts d’horloge en R1. Le commentaire `up.ts:393`, « shrinks it to the syscall », exagère également : la sonde asynchrone, son traitement et la vérification de vivacité précèdent encore le signal.

6. **constat** — `dates this very process to when it actually started` passe encore avec une mutation retournant `Date.now()` si `process.uptime() < 2 s` ; aucun âge minimal n’est imposé. `owns a live pid it CAN date, off this platform` fabrique aussi sa référence avec la fonction testée : cette même mutation peut alimenter simultanément le fichier et la comparaison. Enfin, `owns the pid our lockfile names, and its workers` reste vert si l’ancien filtre de `runUp` omet à nouveau les workers : il n’exerce que le classificateur.

   Vérifications exécutées en mémoire, sans signal ni modification de fichier. Suite Vitest non exécutée dans cette sandbox en lecture seule ; comportement réel après suspension **NON TRANCHÉ expérimentalement**, sémantique vérifiée dans les sources Linux.

des constats