- **R1 — modéré, couverture de régression — `apps/cli/src/tests/postmaster-pid.test.ts:192`, `:206`. Le constat tient : le test de datation peut accepter une réponse « maintenant ».** Si le processus de test a moins de deux secondes, remplacer la datation d’un PID existant par `Date.now()` satisfait encore l’assertion. `3959dcb7` supprime le faux échec lié à l’âge, mais ne garantit plus la détection de cette régression déjà jugée dangereuse. Garantir l’âge du processus mesuré avant la mesure.

- **R2 — modéré, couverture de régression — `apps/cli/src/tests/postmaster-pid.test.ts:119`, `:124`. Le constat tient : le correctif des séparateurs POSIX n’a pas de test discriminant.** Sous Linux, `other` vaut exactement `dataDir` : réintroduire le remplacement des antislashs par `/` laisse ce test passer. Ajouter un cas opposant un antislash littéral à un séparateur. Sous Windows, `.toUpperCase()` subsiste également : la séparation casse/séparateurs annoncée n’est pas complète.

- **R3 — modéré, documentation des garanties — `apps/cli/src/lib/orphans.ts:96`, `:169`, `apps/cli/src/lib/postgres.ts:49`, `:210`, `apps/cli/src/commands/up.ts:327`. Le constat tient : les constats R5 et R6 de passe 9 subsistent.** Le rapport contient **six constats numérotés**, pas quatre, et qualifie R1 et R2 de majeurs. Le commentaire « strictly after » contredit toujours `parent.startedAt <= child.startedAt` ; le résumé du repli présente la vivacité comme suffisante ; la troisième voie est annoncée conditionnelle alors que l’ajout est systématique. Le résidu Windows reste décrit trop étroitement : une différence de création de **+1 500 ms** satisfait le code, sans recul d’horloge. Ces éléments ne sont pas corrigés dans `3959dcb7`.

1. **constat** — Les quatre interventions existent matériellement, mais R4 n’est que partiellement satisfaisant. R1 : relecture à `up.ts:394`, condition avant l’arrêt à `:395`. R2 : normalisation Windows à `postgres.ts:90`, conservation des antislashs POSIX à `:96`. R3 : filtre réel `isPidAlive` à `up.ts:415`, avant l’ajout à `leftAlone`. R4 : chemin adapté à la plateforme à `postmaster-pid.test.ts:124`, suppression de l’exigence d’âge et comparaison à l’âge réel à `:206`. Les deux faux échecs sont supprimés ; les limites sont R1 et R2 ci-dessus.

2. **tient** — Aucun contournement supplémentaire de la lecture fraîche identifié dans le trajet Postgres examiné. La confirmation initiale (`up.ts:257`) commande les candidats issus du port, du fichier et de la table (`:278`, `:305`, `:341`). L’arrêt gracieux exige une nouvelle confirmation (`:394`), puis une concordance du PID du fichier (`postgres.ts:427`). Chaque `SIGKILL` (`up.ts:434`) exige sa propre reconfirmation (`:408`). `leftAlone` et `stillHeld` n’envoient aucun signal. Cela ne supprime pas la course entre lecture et action, notamment la relecture du fichier par `pg_ctl` ; ce n’est pas une garantie d’identité atomique.

3. **tient** — Conserver la lecture supplémentaire est acceptable pour ce nettoyage. Avec un candidat, le trajet comporte trois lectures ; avec N candidats Postgres, N + 2. Sous Windows, chaque sonde possède un délai maximal de dix secondes (`postgres.ts:180`). Aucun cache positif avec une durée non nulle ne préserve la fraîcheur recherchée : un PID peut être recyclé pendant cette durée. Le gain éventuel ne justifie pas de réintroduire cette fenêtre. Latence réelle non mesurée ici.

4. **constat** — R3 ci-dessus. Le message de commit annonce aussi une séparation casse/séparateurs que `.toUpperCase()` contredit encore sous Windows. L’affirmation du test selon laquelle le processus est suffisamment âgé dans toute exécution complète (`postmaster-pid.test.ts:199`) n’est pas garantie.

5. **constat** — Deux exemples précis : « retourner maintenant comme date de création » peut passer `dates this very process to when it actually started` lors d’une exécution rapide ; « transformer les antislashs POSIX en séparateurs » passe `accepts the same directory written with the other separators`. Le manque de test du repli avec deux répertoires différant seulement par la casse, déjà relevé en passe 9, subsiste également.

6. **constat** — Aucun nouveau contournement des confirmations identifié, mais des défauts de couverture et des contradictions persistent. Les cinq fichiers examinés sont identiques entre `3959dcb7` et HEAD. Vérification statique en lecture seule ; suite Vitest non exécutée et aucun signal envoyé.

des constats