node_modules
dist
build
.next
.turbo
.cache
coverage
pnpm-lock.yaml
*.md
playwright-report
test-results
next-env.d.ts

# Generated / build artifacts — regenerated each build, never format-check them
# (prettier does NOT read .gitignore, so gitignored generated files must be
# listed here too or `prettier --check .` flags them locally).
apps/docs/.source/
apps/docs/content/docs/reference/
pack/

# Mesures du portail qualité — écrites par `apps/qa/collect.mjs` à chaque
# collecte, jamais à la main. Les formater ferait diverger le fichier committé
# par la CI de celui produit en local, et chaque nuit rouvrirait le même diff.
apps/qa/data/
